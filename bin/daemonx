#!/usr/bin/env python

from sys import argv

from daemonx.daemon import Daemon


if __name__ == '__main__':
    if '--eventlet_patch' in argv:
        import eventlet
        eventlet.monkey_patch()

    Daemon.run_command(argv[1], argv[2], argv[3:])
